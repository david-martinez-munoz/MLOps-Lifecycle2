{
  "version": "v0.0.1",
  "base_model": "distilbert-base-uncased-finetuned-sst-2-english",
  "format": "safetensors",
  "training_epochs": 0,
  "download_url": "",
  "note": "Modelo base sin fine-tuning. Se descarga automáticamente desde HuggingFace Hub al arrancar la API por primera vez."
}
